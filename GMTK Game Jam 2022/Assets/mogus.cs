using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class mogus : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Application.Quit('A' + 'M' + 'O' + 'G' + 'U' + 'S');
        // ඞ amogus
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
